# cb-agent OTUI 设计 QA

## 对照证据

- 视觉目标：`/Users/cb/Desktop/CbAgent/外部代码/codex-main/codex-rs/tui/styles.md`
- 目标快照：`/Users/cb/Desktop/CbAgent/外部代码/codex-main/codex-rs/tui/src/bottom_pane/snapshots/`
- 实现字符帧：`/Users/cb/Desktop/CbAgent/cb-agent/ui-otui/src/__tests__/__snapshots__/uiVisual.test.ts.snap`
- 视口：`120x36`、`80x24`、`52x18`
- 状态：普通会话、流式输出、工具折叠与展开、差异、Plan、Todo、子 Agent、活动问询、斜杠命令、选择弹窗
- 说明：OpenTUI 的确定性测试输出是逐字符渲染帧，不是浏览器位图；源实现与当前实现均以同尺寸终端字符布局和 ANSI 样式语义进行对照。

## 全屏对照

- `120x36`：完整显示会话标题、模型、模式、权限、会话 ID、剩余上下文、输入/输出 Token 和 MCP，消息区保持单列全宽。
- `80x24`：会话 ID 与 Token 明细按断点折叠，保留模型、模式、权限、上下文和 MCP；输入区与消息区没有横向重叠。
- `52x18`：标题收缩为产品名，底栏仅保留模式、权限和剩余上下文；无固定侧栏，正文、输入区和滚动条均在视口内。
- 活动问询固定在输入区上方，编辑器在问询或弹窗激活时卸载，未观察到光标缓冲覆盖或底部内容被挤出屏幕。
- 选择弹窗在 `80x24` 中使用终端默认背景、弱边框和无填充高亮，尺寸未超过视口可用区域。

## 聚焦对照

### 字体与排版

- 终端字体沿用用户配置；标题和当前选择使用粗体，次要信息使用 `DIM`，正文使用默认字重。
- 用户与输入使用 `›`，助手与运行状态使用 `•`，Thought、工具、Plan、Todo 和子 Agent 使用树状缩进。
- 52 列快照中中文内容正常换行，未发现文字覆盖、负字距或随视口缩放字号的问题。

### 间距与布局

- 已移除 42 列固定侧栏，主区域改为单列全宽结构。
- 消息块不再使用卡片背景；仅展开的工具内容、差异、活动问询和选择弹窗保留必要结构线。
- 输入区为透明多行编辑器，动态高度限制在 3–8 行范围；命令列表最多显示 8 项并围绕当前选项滚动。

### 颜色与视觉标记

- 前景与背景跟随终端默认色，不再固定 Tokyo Night 蓝紫色。
- 青色用于输入、选择和主要交互，洋红用于 Agent 与 Plan，绿色用于成功，红色用于错误。
- 未使用 Codex 规范规避的蓝色、黄色、固定黑色或固定白色前景；次要内容优先通过 `DIM` 弱化。
- 深浅终端的可读性由默认前景/背景与 ANSI 索引色保证；真实启动验证确认 OpenTUI 正确输出默认背景和索引色。

### 图片与资产

- 终端 UI 不包含图片、插画、位图背景或品牌资产，本项不适用。
- 附件以紧凑文件行呈现，不使用占位图形代替真实附件信息。

### 文案与内容

- 会话标题、状态栏、问询说明、命令描述和快捷键文案均保留原有功能含义。
- 模式、权限、上下文、Token 和 MCP 信息按照视口宽度分级折叠，没有引入新命令或修改后端协议。

## Findings

未发现 P0–P2 级设计偏差。当前实现与 Codex 目标在信息层级、终端原生配色、消息前缀、输入组织、选择反馈和响应式折叠方面一致。

残余风险：字符快照无法模拟每一种用户自定义 ANSI 调色板；本轮已通过默认终端真实启动验证，并通过避免固定前景/背景色降低该风险。

## Patches Made

- 重建终端原生主题与 Markdown 显式语法样式。
- 删除固定侧栏，新增紧凑会话标题和三档响应式底栏。
- 重构消息、工具、Plan、Todo、子 Agent、问询、输入区、命令列表和选择弹窗。
- 新增响应式纯函数测试与 9 份确定性视觉快照。
- 将 OpenTUI Solid 预加载统一配置到 `bunfig.toml`，修复测试类型检查冲突。
- 修复输入前缀的尾随空白。

## Verification

- `bun test`：22 通过，0 失败，9 份快照。
- `bunx tsc --noEmit`：通过。
- `git diff --check -- ui-otui`：通过。
- `CBAGENT_WORKSPACE=/tmp/cb-agent-ui-qa bun start`：真实 TUI 成功启动，`Ctrl-C` 干净退出，退出码 0。

final result: passed

# cb-agent 项目介绍

> 一个可拆解、可审计、带工具循环的 LLM Agent 框架。
> 默认走轻量 Markdown 记忆和知识库关键词检索；旧向量记忆/RAG/embedding 只有在设置 `CBAGENT_ENABLE_FULL_MEMORY=1` 后才会启用。

## 架构概览

```
用户输入
  └─> ContextBuilder(Section / Boundary)
        ├─ system / session state / 三层 Markdown memory / knowledge / history
        ├─ full memory + RAG（可选）
        └─ tools + MCP + skills
  └─> LLM stream think
  └─> Function Calling 工具循环
  └─> final answer + 工作记录 + 本地会话持久化
```

## 能力总览

| 能力 | 说明 |
|---|---|
| 多轮 Function Calling | 支持流式内容、reasoning delta、tool_calls 分片累积与 tool result 回灌 |
| 高缓存命中上下文 | System message 静态化，动态内容注入独立 user 消息；Section 级 LRU 缓存，跨轮复用 provider KV cache |
| Hooks 生命周期钩子 | 6 个事件钩子（SessionStart/UserPromptSubmit/PreToolUse/PostToolUse/PreCompact/Stop），双向可阻断，支持命令注入与改写 |
| ContextBuilder | Section / Boundary 架构：静态段可缓存，动态段每轮刷新，按 token 预算组织上下文 |
| 轻量 Markdown 记忆 | 默认启用，按全局、项目、短期三层加载，并接入 `~/knowledge/` 知识库 |
| full 记忆/RAG | 旧 `MemoryTool` / `RAGTool` 完整保留，通过环境变量启用 |
| 跨轮工作上下文 | 每轮工具轨迹压成【工作记录】，写入 `.cbagent/sessions/`，重启后可恢复 |
| 多会话隔离 | 本地 session 可创建、切换、清理；不同会话 history/state/transcript 隔离 |
| 上下文压缩 | OTUI 支持 `/compact`；后端在上下文接近模型窗口 80% 时自动 compact |
| 多模态输入 | 图片可原生发给多模态基模，纯文本基模自动 OCR，音频统一 ASR 成文本 |
| OTUI | OpenTUI + Solid.js 终端界面，支持工具卡片、会话切换、日志面板 |
| QQ / NapCat | `--transport qq` 接入 OneBot V11 反向 WebSocket，群聊/私聊隔离，敏感权限门禁 |
| 微信 OC | `--transport wechat` 接入个人微信 OC HTTP 长轮询，扫码登录、媒体 CDN 上传 |
| MCP | 读取 `mcp.json`，通过 stdio 启动 MCP server 并展开成可调用工具 |
| Skills | Markdown + YAML frontmatter 描述能力，按需加载指令和脚本 |
| Bash 权限 | Bash 工具支持权限语义，OTUI 模式下权限确认走 UI 通道 |

## 平台展示

### OTUI 界面

![OTUI 界面](../img/opentui界面.png)

### QQ 群聊交互

![QQ 对话示例 1](../img/真实素材2-qq1.jpg)

### 微信私聊交互

![微信对话示例](../img/真实素材2-微信1.jpg)

## 相关文档

- [部署与配置](部署与配置.md) — 环境准备、安装步骤、配置说明、OTUI/QQ/微信启动
- [功能详解](功能详解.md) — 记忆系统、会话管理、工具系统、MCP、Skills
- [开发指南](开发指南.md) — 项目结构、设计要点、开发命令、测试、FAQ

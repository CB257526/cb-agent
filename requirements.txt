# cb-agent core 依赖
# 跑通 REPL（agent + context + tools 原生工具 + MCP）所需的最小集合
# 不含本地 embedding、多模态、向量库、图库、PDF skill、外部搜索
#
# 安装方式：
#     pip install -r requirements.txt
# 或者直接装项目：
#     pip install -e .
#
# 完整能力请用 requirements-full.txt 或 pip install -e ".[full]"

numpy>=1.26
pydantic>=2.0
openai>=1.40
python-dotenv>=1.0
tiktoken>=0.7
fastmcp>=2.0
requests>=2.31
colorama>=0.4.6
pyyaml>=6.0
bashlex>=0.18
websockets>=13.0
pycryptodome>=3.20
qrcode>=7.4
Pillow>=10.0
pywebview>=6.2
qtpy>=2.4
PySide6>=6.7
pynput>=1.8
json5>=0.9
